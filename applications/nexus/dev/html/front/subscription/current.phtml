<ips:template parameters="$subscription" />
{{if $package = $subscription->package}}
	<div class="ipsBox cSubscription_current ipsPad">
		{$subscription = $subscription->currentBlurb()}
	</div>
{{endif}}